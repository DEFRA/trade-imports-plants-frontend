// Canonical delimiter for the segments of a composite fulfilment index.
// Model lives here because it owns composite instance-path storage;
// bridge imports downward for its own id formatting/parsing.
export const INDEX_DELIMITER = '.'
